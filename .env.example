# Template for local environment overrides.
# Copy this file to .env.local (untracked, gitignored) and adjust values for your machine.
# .env.local takes precedence over .env / .env.[mode] — see README.md "Environment variables".

VITE_APP_NAME="PWA Boilerplate"
VITE_APP_DESCRIPTION="Single page progressive web application boilerplate using Vite + React + TypeScript + Tailwind CSS + Material UI."
VITE_API_BASE_URL="/api"
